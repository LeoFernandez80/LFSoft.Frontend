# Users Library

This library provides basic users-related services and modules. Import it in the application using the path mapping `@lib/users`.

Files:
- `src/lib/users.module.ts` - Angular module for the users library.
- `src/lib/users.service.ts` - Simple injectable service exported by the public API.
