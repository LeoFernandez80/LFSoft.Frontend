# Purchases Library

This library provides basic purchases-related services and modules. Import it in the application using the path mapping `@lib/purchases`.

Files:
- `src/lib/purchases.module.ts` - Angular module for the purchases library.
- `src/lib/purchases.service.ts` - Simple injectable service exported by the public API.
