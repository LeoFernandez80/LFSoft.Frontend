# Articles Library

This library provides basic articles-related services and modules. Import it in the application using the path mapping `@lib/articles`.

Files:
- `src/lib/articles.module.ts` - Angular module for the articles library.
- `src/lib/articles.service.ts` - Simple injectable service exported by the public API.
